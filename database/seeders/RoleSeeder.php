<?php

namespace Database\Seeders;
//Les seeders permettent de remplir la base de données avec des données de départ nécessaires pour le bon fonctionnement de l'application
use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;
use App\Models\Role;

class RoleSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        /*  $admin=new Role(); 
        $admin->libelle="administrateur(trice)";
        $admin->save();

        $client= new Role();
        $client->libelle="client(e)";
        $client->save(); */
    }
}
